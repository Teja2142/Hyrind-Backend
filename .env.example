# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=True

# Email Configuration
# For Gmail, you need to use an App Password (not your regular Gmail password)
# Generate one at: https://myaccount.google.com/apppasswords
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password-here
DEFAULT_FROM_EMAIL=your-email@gmail.com

# Operations team email (receives interest form notifications)
OPERATIONS_EMAIL=hyrind.operations@gmail.com

# For development/testing without sending real emails, you can use:
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
